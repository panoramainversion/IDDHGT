# A Dynamic Partition Framework for the Riemann Zeta Function via Finite Euler Products
## Abstract
This article introduces a dynamic partition framework for the Riemann Zeta function \zeta(s). By defining a truncation integer N, we partition the natural numbers into three mutually exclusive subsets, yielding a decomposition of \zeta(s) into three regional functions: R_1, R_2, and R_3. We introduce a differential expansion operator \Phi to describe the evolution of the system as N \to \infty. This framework provides a novel dynamical systems perspective on the migration of zeros towards the critical line \mathrm{Re}(s) = \frac{1}{2}.
## 1. Preliminaries and Notations
Let s = \sigma + it \in \mathbb{C} with \sigma > 1. Let \mathbb{P} denote the set of all prime numbers. We define the following fundamental sets and arithmetic functions relative to a strictly positive truncation integer N \in \mathbb{Z}^+:
 * **Truncated Prime Set:**
   
 * **p-adic Valuation:**
   
 * **N-Smooth Numbers:**
   
   
   The set of positive integers whose prime factors are all bounded by N.
## 2. The Three Regional Functions
Based on the canonical prime factorization theorem, we partition the domain of the Dirichlet series of \zeta(s) into three regional generating functions R_i(s; N).
### 2.1 The Core Region (Divisors Domain)
We define R_1(s; N) as the sum over all divisors of N. By the multiplicativity of Dirichlet series over coprime factors, we have the finite Euler product identity:

### 2.2 The Transition Region (N-smooth non-divisors)
We define R_2(s; N) as the sum over N-smooth numbers that do not divide N. This represents the residual of the truncated Euler product:

### 2.3 The Global Residue Region (Prime > N inclusion)
We define R_3(s; N) as the sum over all integers containing at least one prime factor strictly greater than N:

### 2.4 The Global Partition Theorem
**Theorem 2.1.** For any integer N \geq 1 and any complex number s such that \mathrm{Re}(s) > 1, the Riemann Zeta function admits the exact decomposition:

## 3. The Differential Expansion Operator \Phi
To quantify the evolution of the system from a state N to N' (where N' > N), we define the multiplicative expansion operator \Phi_{N \to N'}(s).
**Definition 3.1.**

The operator encapsulates two distinct evolutionary paths in the arithmetic structure:
 * **Path A (Dimensional Activation):** Introduction of a new prime q \in \mathbb{P}_{N'} \setminus \mathbb{P}_N with valuation k = v_q(N').
   
 * **Path B (Valuation Deepening):** Incrementing the exponent of an existing prime p \in \mathbb{P}_N by \Delta k.
   
The total evolution is governed by the relation: R_1(s; N') = R_1(s; N) \cdot \Phi_{N \to N'}(s).
## 4. Asymptotic Convergence
By analytic continuation, the identities hold in the meromorphic domain of \zeta(s). We establish the limits as the truncation parameter approaches infinity.
**Theorem 4.1 (Asymptotic Limits).**
Let N \to \infty under the condition that \min_{p \leq N} (v_p(N)) \to \infty. Then, for s \neq 1:
 1.  2.  3. *Proof Sketch:* The conditions ensure that both the maximal prime P_{\max} \to \infty and the truncation threshold for powers of all local primes tend to infinity. Consequently, S_N \to \mathbb{Z}^+ and the R_1 domain encompasses all n \in \mathbb{N}, forcing the residual partitions to vanish.
## 5. Zero-Migration Dynamics
This framework reformulates the search for non-trivial zeros \rho = \frac{1}{2} + it into a dynamic perturbation problem.
### 5.1 Local Zeros of the Core Region
Let Z_N denote the zero set of R_1(s; N).


From the factorized form of R_1, the equation 1 - p^{-s(a_p+1)} = 0 strictly restricts the roots to the imaginary axis.
**Proposition 5.1.** All elements of Z_N lie on the line \mathrm{Re}(s) = 0, given by s_{k,p} = \frac{2\pi i k}{(a_p+1)\ln p} for k \in \mathbb{Z} \setminus \{0\}.
### 5.2 The Interference Equation
A global zero \zeta(s) = 0 occurs if and only if:

**Dynamical Hypothesis:**
As N \to \infty, the roots s_{k,p} \in Z_N initially situated at \sigma = 0 are subjected to the phase interference generated by the residue term R_3(s; N) (the high-frequency prime noise). The symmetry of the functional equation implies that the attractive fixed points of this zero-migration flow exist exclusively on the critical line \mathrm{Re}(s) = \frac{1}{2}.
## 6. Future Work & Computational Verifications
 1. **Phase Shift Expectation:** Rigorous proof of the bounds for \arg(\Phi_{N \to N'}(s)) to show a leftward phase drift for \sigma > 1/2 and a rightward drift for \sigma < 1/2.
 2. **Numerical Flow Analysis:** Mapping the loci of local minima of |R_1(s; N) + R_2(s; N)| as N ranges over primorials P_k\#, tracking their geometric convergence towards known non-trivial zeros (e.g., s_1 \approx 0.5 + 14.1347i).
